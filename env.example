# ==========================
# Grafana FastMCP .env model
# ==========================
# Copie este arquivo para `.env` e ajuste os valores conforme seu ambiente.

# --------------------------
# Credenciais Grafana (HTTP)
# --------------------------
# URL base da instância Grafana acessível pelo servidor MCP.
GRAFANA_URL=http://localhost:3000

# Token de service account recomendado (Bearer token).
GRAFANA_SERVICE_ACCOUNT_TOKEN=

# Chave de API legada (apenas para compatibilidade; evite usar).
GRAFANA_API_KEY=

# Credenciais para autenticação básica (caso necessário).
GRAFANA_USERNAME=
GRAFANA_PASSWORD=

# Tokens OIDC opcionais.
GRAFANA_ACCESS_TOKEN=
GRAFANA_ID_TOKEN=

# --------------------------
# Configuração do servidor MCP
# --------------------------
# Endereço e porta de escuta do servidor (HOST:PORT).
APP_ADDRESS=0.0.0.0:8000

# Caminho base para os transportes HTTP.
BASE_PATH=/

# Caminho para o endpoint Streamable HTTP (relativo ao BASE_PATH).
STREAMABLE_HTTP_PATH=mcp

# Nível de log global (DEBUG, INFO, WARNING, ERROR).
LOG_LEVEL=INFO

# Transporte MCP padrão: sse | streamable-http | stdio.
TRANSPORT=streamable-http

# Caminho para um arquivo .env alternativo (opcional; útil em scripts).
ENV_FILE=

# Caminho opcional para um arquivo de instruções customizadas (Markdown ou texto).
MCP_INSTRUCTIONS_PATH=

# --------------------------
# Timeouts Streamable HTTP (em segundos)
# --------------------------
# Tempo máximo que a conexão permanece aberta sem tráfego.
MCP_STREAMABLE_HTTP_TIMEOUT_KEEP_ALIVE=65

# Tempo máximo para enviar notificações antes de encerrar a conexão.
MCP_STREAMABLE_HTTP_TIMEOUT_NOTIFY=120

# Tempo máximo para desligamento gracioso do servidor HTTP.
MCP_STREAMABLE_HTTP_TIMEOUT_GRACEFUL_SHUTDOWN=120

# --------------------------
# TLS / SSL (Grafana)
# --------------------------
# Arquivos de certificado/cliente usados ao conectar ao Grafana (opcional).
# Se sua instância Grafana usa certificados auto-assinados, você pode escolher
# pular a verificação TLS ou fornecer um CA bundle personalizado.
GRAFANA_TLS_CERT_FILE=
GRAFANA_TLS_KEY_FILE=
GRAFANA_TLS_CA_FILE=
# Defina como true/1/yes para pular a verificação do certificado TLS (inseguro).
GRAFANA_TLS_SKIP_VERIFY=false
